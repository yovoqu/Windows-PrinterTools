<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WindowsPrinter.Views.Components.PrintLogPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:WindowsPrinter.Models"
    xmlns:tv="using:WinUI.TableView"
    xmlns:vm="using:WindowsPrinter.ViewModels"
    d:DataContext="{d:DesignInstance vm:PrintLogViewModel, IsDesignTimeCreatable=False}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Stretch"
    mc:Ignorable="d">

    <Grid Padding="16,12,16,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="会话记录" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="{Binding SummaryText}"
                    TextWrapping="WrapWholeWords" />
            </StackPanel>
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    AutomationProperties.Name="打开日志文件"
                    Content="打开日志"
                    Command="{Binding OpenLogFileCommand}" />
                <Button
                    AutomationProperties.Name="清空日志"
                    Content="清空"
                    Command="{Binding ClearLogCommand}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <tv:TableView
                AutoGenerateColumns="False"
                CanResizeColumns="True"
                GridLinesVisibility="Horizontal"
                IsReadOnly="True"
                ItemsSource="{Binding Entries}"
                SelectionMode="None"
                Visibility="{Binding HasEntries, Converter={StaticResource BoolToVis}}">
                <tv:TableView.Columns>
                    <tv:TableViewTextColumn Header="时间" Binding="{Binding TimeText}" Width="72" />
                    <tv:TableViewTextColumn Header="级别" Binding="{Binding LevelText}" Width="56" />
                    <tv:TableViewTextColumn Header="文件" Binding="{Binding FileName}" Width="100" />
                    <tv:TableViewTextColumn Header="消息" Binding="{Binding Message}" Width="*" />
                </tv:TableView.Columns>
            </tv:TableView>

            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="打印操作将记录在此"
                Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}" />
        </Grid>
    </Grid>
</UserControl>
